import { HeroSection } from "@/features/landing/sections/hero-section";

// Landing page for Loops, the holding company: a single orb-centered hero.
// Deliberately no product sections and no sign-in/sign-up entry points.
export default function Page() {
	return <HeroSection />;
}
